# whitelist
branches:
  only:
    - master

language: objective-c
before_install:
    - brew update
    - brew uninstall xctool && brew install xctool
    - sudo easy_install cpp-coveralls
script:
    - make test
after_success:
    - make send-coverage